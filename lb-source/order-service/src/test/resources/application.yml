load-balancer:
  url: http://localhost:9500/

retry-config:
  max-retries: 3
  delay: 1
  max-duration: 30

order-service:
  zone: Europe

server:
  port: 9000

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
